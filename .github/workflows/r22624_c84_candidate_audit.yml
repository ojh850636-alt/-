name: R22624 C84 focused metadata audit (frozen)
on:
  push:
    branches: [r22624-c84-focused-disabled]
jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Focused candidate audit completed; no rerun on active branch.'
