name: R22574 C58 behavior causal (transport repair gate)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  guarded:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Behavior retry is gated while the exact runtime transport is rebuilt from verified chunks.'
          echo 'No Base bytes/model output were consumed by run 32147568921.'
          exit 1
