name: R22574 C58 PorTAL RTE behavior causal (manual after metadata amendment)
on:
  workflow_dispatch:
permissions:
  contents: read
  actions: write
jobs:
  guarded:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Behavior campaign temporarily gated pending exact Base metadata amendment.'
          echo 'Do not dispatch until r22574/C58_BASE_METADATA_PREINGRESS_AMENDMENT.json is bound.'
          exit 1
