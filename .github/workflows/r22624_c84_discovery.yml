name: R22624 C84 CPU metadata-only broad discovery (frozen)
on:
  push:
    branches: [r22624-c84-broad-disabled]
jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Broad discovery already completed; no rerun on active branch.'
