name: LUCIA AA C4 safety report retired

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired after exact Gate JSON was verified from raw-free artifact 8477158363.'
