name: R22580 C62 Task Planner One-Use Archaeology (transport staging)
on:
  workflow_dispatch:
jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'C62 v3 source-zero bundle staging only; no Adapter/Base ingress.'
