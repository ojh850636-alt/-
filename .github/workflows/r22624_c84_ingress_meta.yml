name: R22624 C84 ingress metadata (frozen)
on:
  push:
    branches: [r22624-c84-ingress-meta-disabled]
jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Exact ingress metadata already sealed; no active-branch rerun.'
